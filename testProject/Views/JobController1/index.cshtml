@using EntityLayer.Concrete
@model List<Job>;
@{
    ViewData["Title"] = "index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row">
        <div class="col">
            <a asp-action="AddJob" class="btn btn-outline-warning">Yeni Meslek Ekle</a>
        </div>
    </div>


    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Meslek Adı</th>
                <th scope="col">Sil</th>
                <th scope="col">Güncelle</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td scope="row">@item.meslek_id</td>
                    <td scope="row">@item.meslek_name</td>
                    <td>
                        <a asp-action="DeleteJob" asp-route-id="@item.meslek_id" class="btn btn-outline-danger">Sil</a>
                    </td>
                    <td>
                        <a asp-action="UpdateJob" asp-route-id="@item.meslek_id" class="btn btn-outline-success">Güncelle</a>
                    </td>
                </tr>


            }


        </tbody>
    </table>
</div>

